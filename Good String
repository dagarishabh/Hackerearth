#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    char s[100001];
    cin>>s;
    int i,l=strlen(s),k=1;
    sort(s,s+l);
    for(i=1;i<l;i++)
    {
        if(s[i]!=s[i-1])
        k++;
    }
    cout <<l-k<<endl ;
    return 0;
}
Language: C++
